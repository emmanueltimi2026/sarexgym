import React,{useEffect,useState}from'react';import{CalendarDays,MapPin,Plus,Users}from'lucide-react';import{AppLayout}from'../../components/layout/AppLayout';import{Modal}from'../../components/ui/Modal';import{ConfirmDialog}from'../../components/ui/ConfirmDialog';
const BASE=(import.meta.env.VITE_API_BASE_URL||'http://localhost:8080').replace(/\/$/,'');
const empty={title:'',description:'',location:'',startsAt:'',endsAt:'',capacity:40,price:0,audience:'members',status:'draft',imageUrl:''};
export const EventManagement:React.FC=()=>{const[events,setEvents]=useState<any[]>([]),[open,setOpen]=useState(false),[form,setForm]=useState(empty),[message,setMessage]=useState(''),[pendingAction,setPendingAction]=useState<{id:string;status:string;title:string}|null>(null);const load=()=>fetch(`${BASE}/api/v1/events`,{credentials:'include'}).then(r=>r.json()).then(b=>setEvents(b.data||[]));useEffect(()=>{void load()},[]);const save=async(e:React.FormEvent)=>{e.preventDefault();setMessage('Saving…');const body={...form,startsAt:new Date(form.startsAt).toISOString(),endsAt:new Date(form.endsAt).toISOString(),capacity:Number(form.capacity),price:Number(form.price)};const r=await fetch(`${BASE}/api/v1/events`,{method:'POST',credentials:'include',headers:{'Content-Type':'application/json'},body:JSON.stringify(body)});const b=await r.json().catch(()=>({}));if(!r.ok)return setMessage(b?.error?.message||'Unable to save event');setOpen(false);setForm(empty);setMessage('');await load()};const status=async(id:string,value:string)=>{await fetch(`${BASE}/api/v1/events/${id}`,{method:'PATCH',credentials:'include',headers:{'Content-Type':'application/json'},body:JSON.stringify({status:value})});await load()};return <AppLayout pageTitle="Events" pageSubtitle="Create outings and gym events, control availability, and monitor member bookings." breadcrumbs={[{label:'Events'}]} actions={<button onClick={()=>setOpen(true)} className="inline-flex items-center gap-2 rounded-lg bg-[#EF1B23] px-4 py-3 text-xs font-black text-white"><Plus className="h-4 w-4"/>ADD EVENT</button>}><div className="grid gap-5 lg:grid-cols-2">{!events.length&&<div className="col-span-full rounded-2xl border border-dashed border-gray-300 bg-white p-12 text-center"><CalendarDays className="mx-auto h-10 w-10 text-gray-300"/><h2 className="mt-4 text-xl font-black">No events yet</h2><p className="mt-2 text-sm text-gray-500">Create your first event as a draft, then publish it when the details are ready.</p><button onClick={()=>setOpen(true)} className="mt-5 rounded-lg bg-[#EF1B23] px-5 py-3 text-xs font-black text-white">ADD FIRST EVENT</button></div>}{events.map(event=><article key={event.id} className="overflow-hidden rounded-2xl border border-gray-200 bg-white shadow-sm"><div className="h-36 bg-[#151515]">{event.image_url&&<img src={event.image_url} className="h-full w-full object-cover" alt=""/>}</div><div className="p-5"><div className="flex justify-between gap-4"><h2 className="text-xl font-black">{event.title}</h2><span className="text-xs font-bold uppercase text-[#EF1B23]">{event.status}</span></div><p className="mt-2 text-sm text-gray-600">{event.description}</p><div className="mt-4 grid gap-2 text-xs text-gray-600 sm:grid-cols-2"><span className="flex gap-2"><CalendarDays className="h-4 w-4 text-[#EF1B23]"/>{new Date(event.starts_at).toLocaleString()}</span><span className="flex gap-2"><MapPin className="h-4 w-4 text-[#EF1B23]"/>{event.location}</span><span className="flex gap-2"><Users className="h-4 w-4 text-[#EF1B23]"/>{event.registered}/{event.capacity} booked</span><strong>{Number(event.price_minor)?`₦${(Number(event.price_minor)/100).toLocaleString()}`:'Free'}</strong></div><div className="mt-5 flex gap-2">{event.status!=='published'&&<button onClick={()=>setPendingAction({id:event.id,status:'published',title:event.title})} className="rounded-lg bg-[#151515] px-4 py-2 text-xs font-bold text-white">Publish</button>}{event.status!=='cancelled'&&<button onClick={()=>setPendingAction({id:event.id,status:'cancelled',title:event.title})} className="rounded-lg border border-gray-300 px-4 py-2 text-xs font-bold">Cancel event</button>}</div></div></article>)}</div><Modal isOpen={open} onClose={()=>setOpen(false)} title="ADD UPCOMING EVENT" subtitle="Save as draft or publish immediately" maxWidth="lg"><form onSubmit={save} className="grid gap-4 sm:grid-cols-2">{[['title','Event title'],['location','Location'],['startsAt','Starts'],['endsAt','Ends'],['capacity','Capacity'],['price','Price (₦)']].map(([key,label])=><label key={key} className="text-xs font-bold uppercase">{label}<input required type={key==='startsAt'||key==='endsAt'?'datetime-local':key==='capacity'||key==='price'?'number':'text'} value={(form as any)[key]} onChange={e=>setForm({...form,[key]:e.target.value})} className="mt-2 w-full rounded-lg border border-gray-200 p-3 text-sm font-normal"/></label>)}<label className="min-w-0 text-xs font-bold uppercase">Audience<select value={form.audience} onChange={e=>setForm({...form,audience:e.target.value})} className="mt-2 block w-full min-w-0 max-w-full rounded-lg border border-gray-200 bg-white p-3 text-sm font-normal"><option value="members">Members only</option><option value="public">Public</option></select></label><label className="min-w-0 text-xs font-bold uppercase">Publishing<select value={form.status} onChange={e=>setForm({...form,status:e.target.value})} className="mt-2 block w-full min-w-0 max-w-full rounded-lg border border-gray-200 bg-white p-3 text-sm font-normal"><option value="draft">Save as draft</option><option value="published">Publish now</option></select></label><label className="text-xs font-bold uppercase sm:col-span-2">Description<textarea required rows={4} value={form.description} onChange={e=>setForm({...form,description:e.target.value})} className="mt-2 w-full rounded-lg border border-gray-200 p-3 text-sm font-normal"/></label><label onDragOver={e=>e.preventDefault()} onDrop={e=>{e.preventDefault();const file=e.dataTransfer.files[0];if(file&&file.type.startsWith('image/')&&file.size<=2_000_000){const reader=new FileReader();reader.onload=()=>setForm({...form,imageUrl:String(reader.result)});reader.readAsDataURL(file)}}} className="cursor-pointer rounded-xl border-2 border-dashed border-gray-300 bg-gray-50 p-6 text-center text-xs font-bold uppercase sm:col-span-2">Drag and drop event image here, or click to browse<span className="mt-2 block font-normal normal-case text-gray-500">JPG, PNG or WebP, maximum 2 MB</span><input type="file" accept="image/png,image/jpeg,image/webp" className="hidden" onChange={e=>{const file=e.target.files?.[0];if(file&&file.size<=2_000_000){const reader=new FileReader();reader.onload=()=>setForm({...form,imageUrl:String(reader.result)});reader.readAsDataURL(file)}}}/>{form.imageUrl&&<img src={form.imageUrl} className="mx-auto mt-4 h-28 rounded-lg object-cover" alt="Event preview"/>}</label><span className="text-xs text-red-600">{message}</span><button className="rounded-lg bg-[#EF1B23] p-3 text-xs font-black text-white">SAVE EVENT</button></form></Modal><ConfirmDialog open={Boolean(pendingAction)} title={pendingAction?.status==='cancelled'?'Cancel event?':'Publish event?'} message={pendingAction?pendingAction.status==='cancelled'?`Cancel ${pendingAction.title}? Members will no longer be able to book it.`:`Publish ${pendingAction.title}? It will become visible to its selected audience.`:''} confirmLabel={pendingAction?.status==='cancelled'?'Cancel event':'Publish event'} tone={pendingAction?.status==='cancelled'?'danger':'primary'} onClose={()=>setPendingAction(null)} onConfirm={()=>{if(pendingAction){void status(pendingAction.id,pendingAction.status);setPendingAction(null)}}}/></AppLayout>};






