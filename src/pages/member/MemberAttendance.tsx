import React from 'react';
import {CalendarCheck,Clock,MapPin,ShieldAlert} from 'lucide-react';
import {useGym} from '../../context/GymContext';
import {AppLayout} from '../../components/layout/AppLayout';
import {Badge} from '../../components/ui/Badge';
const checkInLabel=(value?:string)=>{if(!value)return'Recorded';const date=new Date(value);return Number.isNaN(date.getTime())?value:date.toLocaleTimeString([],{hour:'2-digit',minute:'2-digit'});};
const isSuccessful=(status?:string)=>String(status||'').toLowerCase()!=='denied';
export const MemberAttendance:React.FC=()=>{const{currentMember,attendance}=useGym();const visits=attendance.filter(a=>a.memberId===currentMember.memberId),successfulVisits=visits.filter(v=>isSuccessful(v.status)),deniedVisits=visits.length-successfulVisits.length,lastVisit=successfulVisits[0]||visits[0];return <AppLayout pageTitle="My Attendance" pageSubtitle="Review your gym visits and check-in history." breadcrumbs={[{label:'Member Portal',path:'/member/dashboard'},{label:'Attendance'}]}><div className="mb-5 grid gap-3 sm:grid-cols-3"><div className="rounded-xl border border-gray-200 bg-white p-4 shadow-xs"><CalendarCheck className="h-5 w-5 text-[#EF1B23]"/><span className="mt-3 block text-[10px] font-bold uppercase tracking-wider text-gray-500">Successful visits</span><strong className="mt-1 block text-2xl">{successfulVisits.length}</strong></div><div className="rounded-xl border border-gray-200 bg-white p-4 shadow-xs"><Clock className="h-5 w-5 text-[#EF1B23]"/><span className="mt-3 block text-[10px] font-bold uppercase tracking-wider text-gray-500">Last activity</span><strong className="mt-1 block text-sm">{lastVisit?.date||'No visits yet'}</strong></div><div className="rounded-xl border border-gray-200 bg-white p-4 shadow-xs"><ShieldAlert className="h-5 w-5 text-[#EF1B23]"/><span className="mt-3 block text-[10px] font-bold uppercase tracking-wider text-gray-500">Denied scans</span><strong className="mt-1 block text-2xl">{deniedVisits}</strong></div></div><div className="overflow-hidden rounded-xl border border-gray-200 bg-white shadow-xs"><div className="flex flex-col gap-2 border-b border-gray-100 p-5 sm:flex-row sm:items-end sm:justify-between"><div><h2 className="font-black">Visit history</h2><p className="mt-1 text-xs text-gray-500">Successful and denied reception scans are listed in order.</p></div><div className="inline-flex items-center gap-2 text-[11px] font-semibold text-gray-500"><MapPin className="h-3.5 w-3.5 text-[#EF1B23]"/>SAREX Magboro</div></div>{visits.length?<div className="overflow-x-auto"><table className="w-full min-w-[680px] text-left text-xs"><thead className="border-b border-gray-100 bg-gray-50 text-[10px] font-black uppercase tracking-wider text-gray-500"><tr><th className="px-5 py-3">Date</th><th className="px-5 py-3">Check-in</th><th className="px-5 py-3">Method</th><th className="px-5 py-3">Status</th><th className="px-5 py-3">Note</th></tr></thead><tbody className="divide-y divide-gray-100">{visits.map(v=><tr key={v.id} className="transition hover:bg-gray-50/80"><td className="px-5 py-3 font-bold text-[#111]">{v.date}</td><td className="px-5 py-3 font-mono font-bold text-gray-700">{checkInLabel(v.time||v.checkInTime)}</td><td className="px-5 py-3 text-gray-600">{v.method||'Reception QR'}</td><td className="px-5 py-3"><Badge variant={isSuccessful(v.status)?'success':'danger'}>{v.status}</Badge></td><td className="max-w-[260px] px-5 py-3 text-gray-500">{v.denialReason||'Access recorded at reception'}</td></tr>)}</tbody></table></div>:<div className="p-12 text-center"><CalendarCheck className="mx-auto h-10 w-10 text-gray-300"/><h3 className="mt-4 font-bold">No attendance recorded</h3><p className="mt-2 text-xs text-gray-500">Your history will appear after your first gym check-in attempt.</p></div>}</div></AppLayout>};
