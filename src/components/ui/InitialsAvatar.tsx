import React,{useState}from'react';
export const InitialsAvatar:React.FC<{src?:string;firstName?:string;lastName?:string;name?:string;className?:string}>=({src,firstName='',lastName='',name='',className='h-8 w-8 text-[10px]'})=>{const[failed,setFailed]=useState(false);const words=(name||`${firstName} ${lastName}`).trim().split(/\s+/);const initials=(firstName?.[0]||words[0]?.[0]||'S')+(lastName?.[0]||(words.length>1?words.at(-1)?.[0]:'')||'');return src&&!failed?<img src={src} onError={()=>setFailed(true)} alt={name||`${firstName} ${lastName}`} className={`${className} rounded-full border border-gray-200 object-cover`}/>:<span aria-label={name||`${firstName} ${lastName}`} className={`${className} grid shrink-0 place-items-center rounded-full bg-[#151515] font-black uppercase text-white`}>{initials}</span>};
