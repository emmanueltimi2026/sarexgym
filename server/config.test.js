import test from 'node:test'; import assert from 'node:assert/strict'; import {loadConfig} from './config.js';
const base={DATABASE_URL:'postgres://db',APP_ORIGIN:'https://gym.example'};
test('production configuration requires payment secret',()=>assert.throws(()=>loadConfig({...base,NODE_ENV:'production'}),/PAYSTACK_SECRET_KEY/));
test('development configuration is normalized',()=>{const value=loadConfig(base);assert.equal(value.PORT,8080);assert.equal(value.secureCookies,false)});
test('development never emits SameSite=None without secure cookies',()=>{const value=loadConfig({...base,COOKIE_SAME_SITE:'none'});assert.equal(value.secureCookies,false);assert.equal(value.COOKIE_SAME_SITE,'lax')});
test('production cross-site configuration is explicit and secure',()=>{const value=loadConfig({...base,NODE_ENV:'production',FRONTEND_URL:'https://sarex.vercel.app',PAYSTACK_SECRET_KEY:'paystack-secret',CSRF_SECRET:'a-secure-random-value-with-at-least-32-characters',CLOUDINARY_CLOUD_NAME:'cloud',CLOUDINARY_API_KEY:'key',CLOUDINARY_API_SECRET:'cloudinary-secret'});assert.equal(value.secureCookies,true);assert.equal(value.COOKIE_SAME_SITE,'none');assert.equal(value.DATABASE_SSL_MODE,'require');assert.equal(value.DATABASE_POOL_MAX,5)});
